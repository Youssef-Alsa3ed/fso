sequenceDiagram
participant browser
participant server

    Note right of browser: User adds a new note
    Note right of browser: Browser updates the DOM with the new note
    Note right of browser: Browser sends a POST request containing the new note
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: Status Code 201: CREATED
    Note left of server: Server tells the browser that the submission succeeded
    deactivate server
